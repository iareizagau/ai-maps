# Daily Post Creator Tasks

## 1. Services Layer

- [x] 1.1 Create `src/apps/blog/services.py` with configuration parameters for NewsAPI and Gemini.
- [x] 1.2 Implement the NewsAPI/RSS fetcher `fetch_latest_tech_news()` to query technical trends with fallback options.
- [x] 1.3 Implement the deduplication analyzer helper to format recent post history into a concise text block.
- [x] 1.4 Implement the trilingual Gemini generator calling `_call_gemini_generate` with the grounded prompt, maps.eus context dossier, and JSON output formatting constraints.
- [x] 1.5 Add validation checks in the service to parse and verify the autogenerated GeoJSON syntax before creating the database record.

## 2. Celery & Asynchronous Scheduling

- [x] 2.1 Create `src/apps/blog/tasks.py` and register the `generate_daily_post` shared task with automatic retry capabilities.
- [x] 2.2 Configure `CELERY_BEAT_SCHEDULE` inside `src/config/settings/base.py` to trigger the daily task automatically.

## 3. Django Admin Improvements

- [x] 3.1 Update `src/apps/blog/admin.py` to include a filter for publication status and creation dates.
- [x] 3.2 Add a custom admin action `make_published` to bulk-publish draft articles, setting their `published_at` timestamp.

## 4. Testing & Verification

- [x] 4.1 Create unit tests in `src/apps/blog/tests/test_daily_post.py`.
- [x] 4.2 Mock NewsAPI responses and test the fallback to RSS feeds.
- [x] 4.3 Test the JSON parser and validation error handling (e.g. invalid GeoJSON recovery).
- [x] 4.4 Verify that the task runs and creates posts in `is_published=False` status by default.
