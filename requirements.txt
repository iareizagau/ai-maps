django>=6.0a1
django-hosts
django-cotton
django-ninja
django-environ
psycopg[binary]
django-extensions
whitenoise
django-allauth
requests
beautifulsoup4
PyJWT
cryptography
Pillow
gunicorn

# GIS
django-leaflet

# Vector search
pgvector

# Mubil — Gemini (embeddings + generation for RAG `ask`)
google-genai>=1.0

# Mubil — corpus ingest for `ask` (HTML main-content + PDF text extraction)
trafilatura>=1.10
pypdf>=4.3

# Mubil — `news` RSS aggregation (NewsAPI used via plain `requests`).
feedparser>=6.0

# Testing
pytest
pytest-django
faker

# Async tasks
celery[redis]>=5.4
redis>=5.0
django-celery-beat>=2.6

# Email
django-anymail[brevo]

# Debugging
debugpy
gpxpy
osmium
